# Lumen onboarding — fix log

Each version below is one fix, applied on top of the previous. Full snapshots of
the files changed in each fix live in `versions/vNN-<slug>/`. `v00-baseline` is
the untouched starting point. Git commits on branch `claude/lumen-onboarding-fixes`
carry the same steps for diffing.

Severity key: B = blocker, C = confusing, M = minor.

| v | Sev | Fix | Files |
|---|-----|-----|-------|
| 01 | M | Remove dead `getSP()` (full duplicate of the server system prompt) and unused `_unusedLegacyClientAPI` stub. Kills prompt drift + shrinks bundle. | src/lumen.jsx |
| 02 | B | Remove false "editable Google Sheets copy / email / consultant sees every update" promise. FinishCard sent-copy + STEP 7 now describe what actually happens (brief sent, consultant follows up in 2 business days). Removed the never-rendering Sheets link button. | src/lumen.jsx, netlify/functions/chat.js |
| 03 | B | Remove pause/resume + auto-save promises (not implemented live) and the "(simulated in demo)" badge shown to real clients. step1Desc (6 langs), stepper badge, prompt START, prompt CHECKPOINT A. NOTE: real resume is a follow-up (needs stable session id + per-turn save + load-on-mount; not verifiable against Blobs here). | src/lumen.jsx, netlify/functions/chat.js |
| 04 | C | Rewrite PACING so it no longer says "then STOP. Wait for the user's next message" after a widget ack (which contradicted FORWARD MOTION and could strand the client). Now: acknowledge + ask the next question in the same turn; only forbids stacking a second widget. | netlify/functions/chat.js |
| 05 | C | Expert flow (STEP 4A) now shows [WIDGET:MARKETS] like the guided flow, so INFERRED SETUP (languages/timezone) and OBJECTIVES have a defined trigger for experts instead of relying on the gap sweep. | netlify/functions/chat.js |
| 06 | C | Add a manual "Review and send your brief" link below the input (shown once progress >= 15% and < 100%). Gives an escape hatch if the model never emits percent:100; ExportModal still gates the actual send on readiness and lets the client fill gaps. | src/lumen.jsx |
| 07 | C | Merge topic sources by name in ExportModal so urls/hashtags/comments from the %%TOPICS%% marker survive even when the topic-cards widget (name/keywords/rationale only) was used. Previously those fields were silently dropped from the brief and workbook. | src/lumen.jsx |
| 08 | C | (A) mergeCdata now unions channels/reports/alerts by name so a partial marker re-emit can't wipe earlier entries. (B) Reports and alerts are now editable in the review modal (add/edit/remove) and flow from that editable state into the brief, giving a human backstop if the model mis-captured them. | src/lumen.jsx |
| 09 | M | (A) sanitizeIn() collapses injected `%%` runs in client-authored text (typed messages, widget payloads, seeded opener) so a client can't corrupt marker parsing. (B) gw/gwp now pick the latest widget submission of a type (by message-index) instead of the first, avoiding stale reads. | src/lumen.jsx |
| 10 | C | Localize widget chrome (buttons, hints, placeholders, tooltips) into all 6 UI languages via a new WI18N table + WL() helper; thread lang into ChipSelector/RankedSelector/UserForm/TopicCards/QueriesWidget. Option values stay English (product taxonomy, stored in English in the brief). Fixes the all-English form inside a non-English chat. | src/lumen.jsx |
| 11 | B | Consultant notes no longer travel in the client link. New netlify/functions/seed.js stores the seed server-side; Sales POSTs it and the link carries only an opaque id (?s=). The chat fetches CLIENT-SAFE fields only (no notes); notes are returned solely to the dashboard (DASHBOARD_TOKEN). Chat function contract unchanged. Record now carries seedId. NOTE: needs a Netlify smoke test (Blobs runtime not exercisable here); the model no longer receives notes at chat time (intentional; notes are now consultant-only). | netlify/functions/seed.js, public/sales.html, src/lumen.jsx |
| 12 | C | Dashboard now renders the consultant handoff (maturity, goal, hesitations, unconfirmed, follow-ups, tips) from the session record, and fetches the consultant notes from the seed store via the dashboard token. Previously both were captured/stored but never shown, so the consultant never saw the briefing. All fields escaped. | public/dashboard.html |
| 13 | M | session.js POST now rejects malformed shapes (non-object session/merged) and caps collection sizes (topics/channels/reports/alerts ≤300, users ≤200, queries ≤80k chars) and requires a string id. Origin check unchanged; dashboard still escapes on render. | netlify/functions/session.js |
| 14 | M | Escape fmtDate output in the dashboard session row (client-controlled sentAt). Defense-in-depth; the kv() path was already escaped. | public/dashboard.html |
| 15 | M | Add noindex to /sales and /dashboard, with in-code notes that they must be gated (Netlify password/Identity) before production. Update README to the new architecture: seed.js, ?s= links, and the required DASHBOARD_TOKEN env var. | public/sales.html, public/dashboard.html, README.md |
| 16 | B | FIX for v08: revert the by-name array union (it collapsed distinct same-author channels like Nike IG/X/TikTok → silent data loss, plus a push-branch dup bug). Channels/reports/alerts REPLACE wholesale again (model re-emits full arrays). Editable reports/alerts from v08 kept as the backstop. Found by adversarial verification. | src/lumen.jsx |
| 17 | M | Translation-quality fixes from native-speaker review: DE whyUsers grammar ("nur Sie ist" -> correct), AR whyTopics tautology (المواضيع/الموضوعات -> "ما سيراقبه"), FR/IT hintSelectAll de-calqued, FR dragPrioritize anglicism removed, ES whyUsers reworded. | src/lumen.jsx |
| 18 | M | Localize the QUERIES-widget file-import feedback (imported / truncated / no-text / too-large / unsupported / read-error) into all 6 languages via a QN18N table + QN() helper with {name}/{n}/{mb} substitution. Closes the main remaining client-facing English flagged by the l10n review. Access-tier labels (Admin/Full Tool/Read-Only) intentionally stay English (Lumen taxonomy); PathChoice is dev-only (never emitted live); RankedSelector aria-labels remain English (minor a11y, documented). | src/lumen.jsx |
| 19 | M | Remove dead code flagged by the regression sweep: the sheetLink state/prop/param (dead since v02 removed the Google Sheets button) and the now-unused WIDGET_HINTS const (replaced by WL localization in v10). | src/lumen.jsx |
| 20 | Feat | Real pause/resume via localStorage (same device). Debounced autosave of the in-progress draft (messages, progress, widgets, cdata, history, language) keyed by link; mount offers "Welcome back / Resume"; draft cleared on send and on "Start fresh". Honest copy restored (step1Desc x6 = "pause anytime, same device"; "Saved on this device" badge). Cross-device would need the server store (follow-up). | src/lumen.jsx |
| 21 | Feat | Real Google Sheet generation. New netlify/functions/sheet.js (service-account auth via node:crypto, Drive v3 multipart upload converting the XLSX to a Sheet, shares with the client email + notification email). handleSend builds the base64 XLSX, calls it best-effort (brief still sends if it fails/unconfigured), stores sheetUrl, and FinishCard shows the editable-Sheet link + honest copy. Dashboard links the Sheet. README documents GOOGLE_* env + governance. UNTESTED here (no Google creds/runtime); needs a deploy smoke test + ISO 42001 sign-off for PII in Drive. | netlify/functions/sheet.js, src/lumen.jsx, public/dashboard.html, README.md |
| 22 | Feat | sheet.js now supports 3 auth paths, selected by env vars: (A) OAuth as a real user (GOOGLE_OAUTH_CLIENT_ID/SECRET/REFRESH_TOKEN) - writes into a My Drive folder owned by an account you control, no admin; (B) service account + domain-wide delegation (GOOGLE_IMPERSONATE_SUBJECT) - impersonate a real user, needs Workspace admin; (C) service account into a Shared Drive. Lets it target the Proserv-owned folder (My Drive) via A or B. README documents all three. Still untested here. | netlify/functions/sheet.js, README.md |
| 23 | Feat | Add the Apps Script Web App path (preferred): new apps-script/onboarding-sheet-webapp.gs runs as your Google account and creates the Sheet in the Proserv folder (reuses the survey script's working auth model, no service account/OAuth/delegation). sheet.js POSTs to APPS_SCRIPT_WEBAPP_URL with APPS_SCRIPT_SECRET when set (takes precedence; Google-API paths remain fallbacks). Chosen because the onboarding flow will replace the retiring survey flow that wrote to the same folder. README updated. | apps-script/onboarding-sheet-webapp.gs, netlify/functions/sheet.js, README.md |
| 24 | Feat | Slack alert to the same channel when a client completes and a brief is created. Posted from the Apps Script Web App (fires at file creation), token in Script Property SLACK_BOT_TOKEN (not hardcoded), channel default C097154H39N (override via SLACK_CHANNEL). No-op if token unset; non-fatal to the Sheet response. Passes company/contact/topics/users counts through handleSend -> sheet.js -> Web App. | apps-script/onboarding-sheet-webapp.gs, netlify/functions/sheet.js, src/lumen.jsx |
| 25 | Feat | Brief now follows the master template exactly. Apps Script Web App COPIES the "Lumen Requirements Document" template (1VC7...) into the Proserv folder and fills responses by matching the template's own labels/headers at runtime: Business Objectives (verified layout), Users, Topics, Social channels (headers from file preview), Reports/Dashboards/Alerts (best-effort, layout not fully verified). Client sends structured brief (merged + users + onboarding language) to sheet.js -> Web App. Google-API fallback paths still emit the generic buildWorkbook layout. | apps-script/onboarding-sheet-webapp.gs, netlify/functions/sheet.js, src/lumen.jsx, README.md |
| 26 | UX | Make the local xlsx download a fallback, not a co-equal option. Once the Google Sheet exists, FinishCard shows only the Sheet link and a "Review" button, and the review modal hides "Download a copy". If no Sheet was created (unconfigured/failed), the download stays as the fallback so the client isn't empty-handed. Pre-send review-modal draft download unchanged. | src/lumen.jsx |
| 27 | Cost | (a) Prompt caching: chat.js sends SYSTEM_PROMPT as a cache_control block (billed ~10% on repeat turns), OVERSTATE_FIX kept as a separate uncached block. (b) chat.js returns data.usage; lumen accumulates input/output/cache tokens in usageRef and stores them on the session record (tokens) so cost/conversation is measurable. (c) Strip hidden <thought> blocks from assistant turns before they re-enter history (markers kept). From the improvement scan (items 10, 13, 11). | netlify/functions/chat.js, src/lumen.jsx |
| 28 | CX | (a) COMPETITORS rule carve-out: confirm competitors by name instead of re-asking cold when the client already named them. (b) Arabic RTL: avatar gap, quick-reply row indent, and "show earlier" indent now use logical properties (marginInlineEnd/Start) so they mirror; LTR unchanged. (c) TopicCards gets tap-friendly ▲/▼ reorder buttons and hides the drag hint on touch (drag was dead on mobile). (d) Retry card copy is now honest/action-oriented instead of implying auto-recovery. Scan items 3, 1, 2, 4. | netlify/functions/chat.js, src/lumen.jsx |
| 29 | Brief | Brief quality, so the sheet the consultant gets is richer and complete. (a) COMPANY marker now carries markets + teams (was inferred but never emitted as structured data); lumen reads them with a `cdata.company` fallback when the widgets weren't used. (b) TOPICS marker gains a `group` field + prompt guidance to categorise topics (Own brand / Competitor / Industry / Campaign), matching the template's Group column; group is preserved through the ExportModal topic merge. (c) NOISE CHECK now folds exclusions into keywords as explicit Boolean NOT clauses (not just a comment) so the feed is actually filtered. (d) REPORTS: a named report/alert recipient must also land in the USERS list with an email, so the consultant has an address to send to. (e) HANDOFF now emits at the STEP 6 summary and refreshes at 100% (and on early/stuck sends) instead of once at 100%; handleSend synthesises a minimal handoff if the model never emitted one, so an early send never leaves the consultant blind. Scan items 5, 6, 7, 8, 9. | netlify/functions/chat.js, src/lumen.jsx |
| 30 | Robust | Send-path robustness so a client never loses a brief or gets stuck. (a) Double-send guard via a ref (React `sending` state lags a fast double-click, so two records / Sheets / Slack alerts could fire from one double-tap). (b) handleSend wrapped in try/finally so the Send button always re-enables — previously an unguarded `buildWorkbook`/`XLSX.write` throw left it stuck on "Sending…" forever. (c) No false success: if BOTH the session store AND the Sheet fail, the client sees an honest retry message and the modal stays open, instead of a "sent" screen for a brief that reached no one. (d) The resume draft is cleared only after the session actually saves — a failed save now stays resumable instead of being wiped. (e) mergeCdata wipe guards: an empty-array marker re-emit no longer erases captured topics/channels/reports/alerts, and a company/handoff re-emit with blank fields no longer nulls values already captured (blanks pruned; true removals still available in the editable modal). (f) All four fetches (chat 60s, sheet 45s, session/seed 15s) now time out via a shared AbortController helper so a hung network can't freeze the UI. Scan items: robustness bucket. | src/lumen.jsx |
| 31 | Feat | Live completion % on the dashboard + status/progress filters, and fixes for 9 adversarially-verified findings. FEATURE: (a) the client autosaves a trimmed in-progress snapshot to the server each turn from the first real answer (status in_progress + percent; messages/history stay local), keyed by session id; the completed record overwrites it on send. (b) The dashboard shows In progress / Stalled (in progress, idle >48h, derived) / Completed with a per-session progress bar, and the completion-rate KPI is now meaningful. (c) Status and Progress filter dropdowns + Clear. FIXES (from two verification passes): BLOCKER stored-XSS via unescaped `apiCalls` in the list table — now coerced to a number in session.js summarize and type-gated on render; NaN-proof cost math (numeric coercion of token fields); KPI corrections (completion rate, median relabeled "start to submit", time-saved over the timed population, $/brief); server status-lock so a late in-progress write can't downgrade a completed brief; "Start fresh" now reuses the session id so it can't orphan a phantom stalled row; renderSheet tolerates malformed records (Array guards + duration coercion); a CSS class collision (`.bar`) that had flattened the progress bars. Verified by headless render (XSS/NaN/filters/stalled all confirmed) + adversarial review. GOVERNANCE: partial client data now persists before "Send" — confirm retention with the ISO 42001 owner before real-client use. | public/dashboard.html, netlify/functions/session.js, src/lumen.jsx |
| 32 | Feat | Richer Slack completion notification. `postCompletionSlack_` now posts a Block Kit message: a two-column field section (Client, Contact, Topics, Users, plus Industry, Markets, and Top objective when known), the Sheet link, and a context line ("Client has edit access to the Sheet until the review call"). All client values are escaped for Slack mrkdwn, and a plain-text fallback is included. Two mock-up elements were deliberately left out because the data does not exist yet: "Link prepared by <name>" (the sales page does not capture the signed-in user — supported if a `preparedBy` field is ever passed) and a "Conversation transcript" link (the transcript is not stored server-side). Requires re-pasting the script into Apps Script and redeploying the Web App. | apps-script/onboarding-sheet-webapp.gs |
| 33 | Feat | "View full session" deep-link in the Slack alert. handleSend now passes the session id to sheet.js, which forwards it to the Apps Script; the Slack message adds a second link `<DASHBOARD_URL>?id=<sessionId>` alongside the Sheet link. The dashboard reads `?id=` on load and auto-opens that session's detail (full brief + handoff + notes), after the usual token prompt. Degrades gracefully to the Sheet link alone if the DASHBOARD_URL Script Property or the id is missing. Chose this over storing the raw transcript so no extra client PII is persisted. Verified: two-link payload builds, and the deep-link auto-opens the detail modal in a headless render. Needs the DASHBOARD_URL Script Property set and the Apps Script redeployed. | src/lumen.jsx, netlify/functions/sheet.js, apps-script/onboarding-sheet-webapp.gs, public/dashboard.html |
| 34 | Feat | IC/TAM @mentions in the Slack alert, ported from the survey script. After the base alert, a threaded reply matches the client name against the pipeline tracker (ClosedWon then Pipeline, fuzzy match on the account column) and @mentions the assigned IC and TAM, with the existing-client / low-confidence / TW-Core notes and an escalation ping for Core or no-match. Ported helpers: normalize/levenshtein/similarity, getSlackMention, findBestMatch; the base post now captures its `ts` so the reply threads under it; all best-effort (a lookup failure or missing config never affects the base alert). GOVERNANCE: the staff roster (name -> Slack id), the tracker id, and the escalation ids are read from Script Properties (PIPELINE_SHEET_ID, SLACK_IDS_JSON, SLACK_ESCALATION), NOT hardcoded, so no employee roster is committed to the repo. Verified with stubbed Apps Script globals: exact/fuzzy/unknown name resolution, tracker match, and the threaded reply text all correct. Requires setting the three Script Properties and redeploying the Web App. | apps-script/onboarding-sheet-webapp.gs |
| 35 | Cost | Cache the conversation history, not just the system prompt. chat.js now puts a cache breakpoint on the last message, so on every call the prior turns are billed at the cache-read rate (~10% of input) instead of full input. The history was previously re-sent at full price on all ~15-25 calls per chat, so this is the largest remaining lever. Pure billing/latency change — the model receives byte-identical tokens, so output quality is unaffected. Verified request shape (prior messages stay plain strings; last message becomes a content-block array carrying cache_control: ephemeral). Measure the real effect via the per-session token split already logged since v27 (input / output / cacheRead / cacheWrite). | netlify/functions/chat.js |
| 36 | B | Fix (scan): a first-turn or resume API failure left a permanent "Assistant is thinking…" spinner with no way out. startConvo/resumeConvo now wrap the call in try/catch/finally so the spinner always clears and a "Try again" card is offered (retries the opener or the resume). resumeConvo clones saved.history (no double [RESUMING SESSION] marker on retry) and only clears the resume draft after a successful continuation. | src/lumen.jsx |
| 37 | C | Proserv scale (scan): dashboard now has a text search (client / contact / email) and sortable columns (Client, Status, Progress, Time, Tokens, Last active) — it did not scale to 20 live onboardings before. Also surfaces the per-session token split (input / output / cached-read) and est. cost in the detail modal, so Proserv can see whether prompt caching is actually landing (cached-read > 0 = cache hits). Search box lives in the filter bar (keeps focus across re-render); headers toggle asc/desc. | public/dashboard.html |
| 38 | C | Sales safety (scan): the generated client link stayed visible/copyable after the form was edited, so a consultant could copy a link pointing to a different client's seed than what was on screen. Editing any field (or Use example) now clears the shown link, forcing a regenerate. | public/sales.html |
| 39 | C/M | Brief completeness (scan): four fields that were captured in chat but dropped from the requirements Sheet now flow through. (a) Industry: added a row to buildWorkbook and a label rule to the Apps Script Business Objectives fill. (b) Objective Details: added a label rule on the Apps Script path (was only in the fallback XLSX). (c) Preferred Onboarding Language: the fallback XLSX no longer hardcodes "English", it uses co.onboardingLanguage. (d) Migrated queries: the Apps Script path had no queries tab (the whole point of the QUERIES widget for expert clients), so it now inserts a "Migrated queries" tab when the client brought queries across. Apps Script label rules fire only if the master template has the matching row; the queries tab is inserted unconditionally when queries exist. | src/lumen.jsx, apps-script/onboarding-sheet-webapp.gs |
| 40 | C | Accessibility (scan): (a) ExportModal (review & send) now has role="dialog" aria-modal, closes on Escape, and moves focus into the dialog on open (was a plain div with no dialog semantics/keyboard escape). (b) ChipSelector options (markets/languages/teams/timezone) now expose aria-pressed for their chosen state. (c) Larger chip touch targets (~24px -> 38px min-height) for phones. (d) The generating/streaming bubble is now a role="status" aria-live region labelled "Assistant is thinking", so screen-reader users aren't left in silence for up to 60s. Full focus-trap in the dialog remains a further enhancement. | src/lumen.jsx |
| 41 | Perf | Lazy-load xlsx (scan): xlsx was statically imported and shipped in the main bundle, downloaded by every client on first paint though it's only used on file upload (QUERIES) or export/send. Now dynamically imported via a loadXLSX() helper at the five use sites (QUERIES import, doExport, handleSend, and the two demo download buttons). Build confirms the split: main chat-main.js dropped from ~704 KB to 275 KB (gzip 231->89 KB); xlsx is a separate 429 KB chunk fetched on demand. Pure quality-neutral perf: identical functionality, faster initial load. Interop (m.default||m) verified to expose read/utils/write/writeFile with a working write round-trip. | src/lumen.jsx |
| 42 | C/M | Robustness (scan): (a) Seed-fetch failure no longer silently downgrades a prepared session and orphans the consultant notes. fetchSeedFromURL now retries once and, if it still fails, KEEPS the seedId (from the ?s= link) so the completed record stays joinable to the seed store; flags seedError. (b) The "Saved on this device" badge is now gated on whether localStorage is actually writable (probed on mount + updated per autosave), so private-mode/quota/disabled-storage clients aren't shown a false persistence promise. | src/lumen.jsx |
| 43 | M | Demo honesty (scan follow-up): the in-app demo Slack card (stage 3) still showed a hardcoded "Link prepared by damien@hootsuite.com" and a "Conversation transcript" link — neither exists in the real message. Aligned it with what the Apps Script actually posts now (v32-34): dropped prepared-by, added Industry/Markets fields, links now read "Open the requirements document · View full session", and added an illustration of the threaded IC/TAM @mention reply. Internal demo only; no production behavior. | src/lumen.jsx |
| 44 | Feat | "Link sent" funnel visibility + seed retention (scan + your go-ahead). (a) seed.js gains a token-gated LIST endpoint (notes excluded) and a lazy TTL: seeds expire after SEED_TTL_DAYS (default 90), enforced on read/list (expired = not-found + deleted; no cron). (b) session.js summary now exposes seedId so the dashboard can match sessions to seeds. (c) dashboard.html fetches the seed list (best-effort) and shows seeds never opened as "Link sent" rows (deduped against sessions by seedId), with a matching status pill + filter option; these rows are non-clickable (no session detail yet) and excluded from the KPI tiles. Verified via headless render: unopened seed shows, opened seed deduped, filter isolates, rows non-clickable, no regressions. | netlify/functions/seed.js, netlify/functions/session.js, public/dashboard.html |
| 45 | Tool | Cost-lever A/B harness (dev only; ships nothing to prod). `tools/ab-harness.mjs` answers "does this lever cut cost WITHOUT cutting quality?" with numbers instead of a guess — the standing rule from "only change it if I'm sure quality won't drop". It reads the LIVE SYSTEM_PROMPT straight out of chat.js (so the test can't drift from prod), runs a full simulated onboarding (assistant vs a client-persona model) for the baseline and each lever variant, then a judge model scores each transcript for coverage/coherence/integrity. Prints an avg-$/convo vs quality/5 table with the delta from baseline. Only ASSISTANT tokens count toward cost (that's what runs live); the system prompt is cached like prod, so the RELATIVE delta is the trustworthy number. Two starter levers included (terser hidden `<thought>`; emit markers only on change). Makes real API calls (a few $/run) — run before touching the live prompt, never in CI. Dry-run verified with a mocked fetch: completion-break path, usage accounting, judge JSON parse, and table print all correct. | tools/ab-harness.mjs |
| 46 | Robust | Send-path durability (scan). (a) Sheet idempotency: the Apps Script now dedups on sessionId (script cache, auto-expiring so no unbounded growth) behind a short script lock, so a slow copy that outlasts the client's 45s timeout and gets retried returns the SAME Sheet instead of copying a duplicate and re-firing the Slack alert. Older clients that send no sessionId behave exactly as before (no dedup). (b) Save-before-Sheet: handleSend now stores the session record BEFORE the Sheet step (which fires the Slack "View full session" deep-link), and passes the sessionId to the Sheet call only when that save succeeded — so a failed save can never advertise a deep-link that 404s. The Sheet link is attached to the record in a second write once it exists. The delivered-gate and no-false-success semantics from v30 are unchanged. Verified: idempotency (retry -> same URL flagged idempotent, zero extra copies/alerts; different session -> new sheet; no-sessionId still works; bad secret rejected) via a stubbed doPost; send ordering + deep-link withholding across the full save/sheet failure matrix (15/15) via a control-flow sim; app build clean. Requires re-pasting the Apps Script and redeploying the Web App. Scan items 16, 19. | src/lumen.jsx, apps-script/onboarding-sheet-webapp.gs |
| 47 | Tool | Extend the A/B harness to make scan finding 21 measurable (sliding MAX_HIST window vs full cached history). (a) The harness now replicates v35's last-message cache breakpoint — it previously cached only the system prompt, so it literally could not see the history-cache effect. (b) Configs gain a `history` strategy: "slide20" (prod today: trims to the last MAX_HIST messages, which slides the prefix and defeats the v35 cache on the back half of long chats) and "full" (stable growing prefix, cache hit every turn). (c) New "full-history" config pits the finding-21 candidate against baseline. Run it before changing the live MAX_HIST_TURNS: full-history is very likely cheaper AND quality-neutral, but the change alters what the model sees, so it falls under "only change if I'm sure" — the harness is how you confirm rather than guess. Dry-run verified (mocked fetch): v35 breakpoint + system cache present on every assistant request, slide20 caps at MAX_HIST, full grows past it, all four configs print. Dev tool; ships nothing to prod. | tools/ab-harness.mjs |
| 48 | Tool | Make finding 21 decidable for QUALITY, not just cost (you asked for more testing that it will not hurt quality). (a) ab-harness.mjs gains a fact-retention probe: the client persona states nine distinctive INVENTED facts early (company, three competitors, campaign, three markets, email), then after the chat the model is asked to read them back using the same history strategy; recall = the fraction it still has. Invented names force true context recall rather than world-knowledge guessing, and recall is far more sensitive than a coarse 1-5 quality score to the exact risk (an early fact dropping out of the window). New recall column + a "Finding 21" verdict line (cost/quality/recall deltas and a READ recommendation). Probe tokens are excluded from cost like the client/judge calls. (b) NEW tools/window-analysis.mjs: an API-free companion that simulates the slice(-MAX_HIST) window over the app's own realistic onboarding (loadTestSession) and reports which early facts leave the model's context by the closing turn. On the built-in conversation it shows slide20 drops the specific markets and the contact email from context at the final summary — the BRIEF is unaffected (those are captured in the %%markers%% client-side when emitted), but late conversational recall is at risk, which is what full-history would fix. Both dev-only, ship nothing. Dry-run verified: recall distinguishes slide20 (0%) from full (100%) on a small window and the verdict prints; window-analysis runs portably from the repo. | tools/ab-harness.mjs, tools/window-analysis.mjs |
| 49 | C | Finding 22A (your call: delete the dead path, do not build real streaming). callAPI took an onDelta callback it never invoked, so streamTxt stayed empty and the loading bubble always showed the plain typing indicator — the streaming UI (progressive text + cursor) and safeVisible() helper were dead code implying a feature that never ran. Removed: the onDelta param from callAPI (and threaded the two remaining call sites), the streamTxt state, the streamTxt render branch (now just <TypingIndicator/>), and the orphaned safeVisible(). No behaviour change (the client already only ever saw the typing indicator); build clean, bundle ~0.5KB smaller. If real streaming is ever wanted, it is a proxy change (finding 22B), tracked separately. | src/lumen.jsx |
| 50 | Sec | Finding 24 (write endpoints open). Both write functions previously let a MISSING Origin header pass, and had no other auth, so any script could POST to the seed/session stores. (a) Origin is now required present AND same-origin on both POSTs (malformed Origin rejected too); if the URL env is unset we log and proceed rather than lock out a misconfigured deploy. Origin is a layer, not a lock (spoofable outside a browser). (b) session POST stays public by necessity (the chat page has no login, so it cannot hold a secret) — documented, with its real defences being validation + caps + the status lock. (c) seed POST gains an OPTIONAL write token: set SEED_WRITE_TOKEN and the Sales page prompts the consultant for it (cached per tab, re-prompted on 401, like the dashboard token) and sends x-app-write-token; unset = origin-only, so nothing breaks until opt-in. No secret is committed to the repo. README rewritten to stop implying page gating protects the function endpoints; DEPLOY.md documents SEED_WRITE_TOKEN. Verified with a vm behavioural test (11/11: missing/wrong/malformed Origin -> 403, good Origin -> 200, no-URL-env proceeds, seed token enforced only when set, Origin still enforced with a valid token) + sales.html inline-JS syntax check. | netlify/functions/session.js, netlify/functions/seed.js, public/sales.html, README.md, DEPLOY.md |
| 51 | Feat | Finding 14 (owner attribution on the dashboard). The Sales page gains a "Prepared by" field (the consultant's name), remembered per device in localStorage and prefilled on load; it is sent on the seed. seed.js stores preparedBy but keeps it OUT of CLIENT_SAFE, so the client chat NEVER receives it — it is returned only to the token-gated dashboard (by-id and list). The dashboard joins preparedBy onto each session by seedId (owner lives only in the seed store, never on the client-facing session record), adds a sortable Owner column, an Owner filter (dropdown of the distinct owners present, shown only when any exist), and includes owner in the search haystack. Owner names are user-entered, so the filter <option> value and the cell are HTML-escaped. DELIBERATELY not threaded into the Slack alert: the alert already @mentions the assigned IC/TAM from the tracker (v34), and adding "prepared by" there would either push the staff name to the client or need a server-side seed fetch — redundant for little gain. Identity source is the typed-and-remembered name (zero infra); upgrade to Netlify Identity later if you want it verified. Verified via headless render (Owner column + per-row owner via seed join + owner filter isolate + XSS-in-owner-name escaped, 0 exec) and a vm test (client seed GET has no preparedBy/notes; dashboard GET has both). | public/sales.html, netlify/functions/seed.js, public/dashboard.html |
| 52 | Feat | Finding 10 (stalled onboardings never alerted). New Netlify SCHEDULED function netlify/functions/stalled-check.js runs hourly, scans the session store for status==in_progress sessions idle longer than STALLED_HOURS (default 24, your call) that were never nudged, and posts ONE Slack alert per session to the same channel as the completion alert (SLACK_CHANNEL, default C097154H39N) with a dashboard deep-link (URL + /dashboard?id=). Each nudged session is stamped nudgedAt so it fires once; a client resuming (which refreshes lastActiveAt) makes it eligible again only after it re-stalls. Data-safety: because Blobs has no CAS, before stamping nudgedAt the function RE-READS the record and writes only if it is still in_progress — so a brief the client completes mid-scan is never overwritten with a stale in-progress copy. No token => no-op; a failed Slack post leaves nudgedAt unset so it retries next run. Registered via its config.schedule export (no netlify.toml change). Needs SLACK_BOT_TOKEN set on Netlify (same token as the Apps Script). Verified with a mocked store+fetch (15/15: eligibility rules, one-shot nudging, deep link, no-token no-op, Slack-failure retry, and the clobber guard skipping a completed re-read). | netlify/functions/stalled-check.js, DEPLOY.md |
| 53 | Tool | Fix the A/B harness bugs surfaced by your AB_RUNS=1 run (quality 0.00 everywhere; recall inverted). (a) JUDGE: max_tokens 400 was truncating the JSON, and the parse was brittle, so every judge call fell back to null -> quality rendered as 0.00. Now: tighter JSON-only rubric, max_tokens 800, tolerant parse (strips ```fences, derives overall from the sub-scores if the model omits it), one retry, and the raw text is logged to stderr on failure. (b) RECALL was confounded by simulation noise: it depended on whether the role-played client happened to state the invented facts, which varied per run and produced the impossible baseline-100/full-0 inversion at N=1. Now the client states all nine facts in a DETERMINISTIC opening turn (identical across configs), and the persona is told not to re-list them, so recall measures ONLY whether the history strategy keeps that turn in context (full keeps it; slide20 loses it once the chat passes MAX_HIST). Missing facts are logged per run. (c) A failed probe/judge now renders "n/a", never a fake 0/0%, and the Finding-21 verdict refuses to decide when quality or recall is unmeasured. Dry-run verified (mocked): fenced+overall-less judge -> 4.33; recall full 100% vs slide20 0% (correct direction); verdict reads adopt; smoke-run note shown. Re-run with AB_RUNS=3+ for a trustworthy answer. | tools/ab-harness.mjs |
| 54 | B | Fix a v51 regression found by adversarial verification: an active Owner filter could hide EVERY row. Owner is joined onto sessions only after the seed fetch resolves (loadLinkSent), but render()->renderTable() runs first, and load()/Refresh does not reset FILTER. So with FILTER.owner set, the initial/Refresh render (and permanently, if the seed fetch fails) saw every session with no owner and dropped all rows -> "No sessions match" while the KPIs still counted them; if the seed fetch failed the Owner dropdown also never rendered, leaving an invisible filter recoverable only via Clear. Fix: apply the owner filter ONLY when owner data is actually present (ownersKnown), matching the exact condition under which the Owner dropdown is shown — so the filter is inert until/unless owners are joined, and never strands the table. Verified via headless render: happy-path owner filter still isolates correctly, and with owners un-joined + a stale owner filter the rows still show (5/5, not empty). | public/dashboard.html |
| 55 | UX | Send-moment clarity (UX scan #0/#6/#15/#10, all CONFIRMED). (a) The pre-send FinishCard no longer reads as a finish line: 🎉 "Setup brief ready" (shown at 100% BEFORE anything reaches Proserv) becomes 📨 "One last step: send your brief"; the ✅ celebration is reserved for the actually-sent state. (b) The review-modal footer now names the actual missing fields ("Still needed: Contact email, Markets +1 more") instead of a bare count, so the reason Send is disabled is always visible — including on touch, where the old title-attr tooltip never showed (tooltips removed). (c) Stale "✓ Ready to download" footer label (left over from pre-v26 when download was primary) corrected to "✓ Ready to send", matching the header card. (d) Pre-send "Download a copy" demoted from a co-equal outlined button next to Send to a quiet underlined text link, so downloading can't be mistaken for submitting; post-send access unchanged (FinishCard). (e) Modal footer now wraps (flexWrap) so the Send button isn't crushed on ~320-360px phones at the conversion moment. | src/lumen.jsx |
| 56 | UX | Widget input UX (UX scan #1/#4/#7/#8, all CONFIRMED). (a) ChipSelector: custom values typed via Add now RENDER as selected chips with a ✕ affordance. Before, they went into the selection but displayed nowhere (the chip row mapped only the preset options), so adding "Mexico" just cleared the input: no confirmation, no typo check, no way to remove. Core path, since the preset market/language/team lists are far from exhaustive. (b) TopicCards: suggested topics now default to KEPT (review-and-discard instead of opt-in-each); the old "pending" default meant Confirm started disabled at "(0)" and any card the client agreed with but never explicitly ticked was silently dropped on Confirm. ✓/✕ now SET kept/discarded (✕ on a discarded card restores it) so there is no third limbo state and what you see is exactly what Confirm submits; added a localized instruction line ("All suggested topics start as kept. Tap ✕ to drop…") and aria-pressed + aria-labels ("Keep/Discard topic X") on the previously unlabelled icon-only controls. (c) UserForm: the disabled Confirm now states its reason next to it (localized: "Each person needs at least a first name and a valid email"), email re-validates on change once an error is showing (blur-only left typers with a dead button and no message), and a missing first name is flagged red once the rest of the row is in use. New WI18N keys (topicHint, confirmUsersHint) in all 6 languages. | src/lumen.jsx |
| 57 | UX | Mobile & layout (UX scan #2/#5/#9/#11/#12/#13, all CONFIRMED). (a) KEYBOARD FIX: the app roots (LiveChat, Demo, the pre-mount loading screens) now use 100dvh where supported (VH_FULL helper; 100vh fallback = status quo on very old browsers). With plain 100vh, iOS Safari does not shrink the locked layout when the on-screen keyboard opens, so the composer + send button sat behind the keyboard during the core interaction. (b) The "Saved on this device" badge now shows on phones too (compact "✓ Saved"): it was desktop-only, so the mostly-mobile audience never got the safe-to-leave reassurance the welcome screen promises. (c) RankedSelector (OBJECTIVES, hit by every guided client) brought up to the v40 touch baseline: option chips minHeight 38 (were ~26px) + aria-pressed, reorder/remove controls 24px -> 32px. (d) iOS zoom-on-focus killed: a mobile media rule forces 16px font on all inputs/textareas/selects under 640px (the composer already did this; the modal fields and widget inputs did not, so focusing them left the page zoomed-in and panning sideways). (e) Channel/report/alert editor rows in the review modal now wrap (flex-basis 140px) instead of cramming four ~55px unreadable inputs into one row on a phone; matches the Users grid behaviour. (f) The stuck-client escape hatch ("Finished early, or stuck? Review and send your brief") is now a visible tap-sized secondary button instead of 11px faint underlined text. | src/lumen.jsx |
| 58 | UX | Copy & states (UX scan #14/#16/#29, all CONFIRMED; #29 from the visual screenshot pass). (a) "Start fresh" no longer erases a saved draft on one tap: it is now "Start over" -> an explicit inline confirm ("Starting over permanently erases your saved answers…") with "Keep my progress" as the safe primary and "Erase and start over" in destructive styling. One stray tap next to Resume could previously wipe an ~80%-done draft with no undo, contradicting the pause-anytime promise. Also, a saved-at-0% draft no longer greets the client with "0% complete" (shows "Your answers are saved on this device" instead). (b) New branded BootScreen (Lumen tile + spinner + "Setting up your session…") replaces the bare unbranded grey "Loading…" that a real client could stare at for up to ~30s (seed fetch: 15s timeout + one retry) as the FIRST thing after tapping their emailed link; also used for the pre-mount draft check. Spinner gains a dark variant (the original was white-on-purple only, invisible on a light background); BootScreen carries its own spin keyframes since it renders before the app's style tag. (c) Welcome step 1 no longer repeats its own title: "About 15 minutes" was both the title and the first sentence of the body, in ALL six languages — the duplicated sentence is dropped (EN/FR/DE/ES/IT/AR). | src/lumen.jsx |
| 59 | A11y | Accessibility beyond v40 (UX scan #17/#18/#19/#20/#23/#24/#25, all CONFIRMED). (a) Focus TRAP in the review modal: Tab/Shift+Tab now cycle within the dialog (previously tabbed straight out into the chat composer behind the overlay) and focus is RESTORED to the opener on close; Escape/open-focus from v40 unchanged. (b) aria-pressed added where v40 missed it: RankedSelector option chips (landed with the v57 chip restyle) and the Admin/Full Tool/Read-Only access toggles in both UserForm and the review modal. (c) Accessible names on text inputs: the chat composer gets aria-label "Type your reply"; the review modal's user/channel/report/alert fields swap RAW-KEY placeholders ("firstName", "author", "owned") for human labels ("First name", "Name / handle", "Owned or competitor?") used as both placeholder and aria-label; fld() fields and UserForm inputs get aria-labels from their visible labels. (d) Remaining icon-only ✕ buttons labelled: remove user/topic/channel/report/alert (TopicCards keep/discard were labelled in v56). (e) prefers-reduced-motion now also governs the JS auto-scroll (scrollIntoView behavior auto instead of smooth — the CSS query cannot reach an explicit JS option). (f) AA contrast: #94a3b8 informational text darkened to #64748b (chip counters, the Optional pill on its grey chip, all five modal empty-states, the modal close ✕, the ✎ Edit affordance) and timestamp opacity raised 0.6 -> 0.85; disabled-state colours deliberately untouched. (g) Heading structure: the welcome and welcome-back titles are now the page h1, the modal title is an h2, and each modal Section title is an h3 wrapping the existing toggle button (valid HTML, zero visual change) so rotor/heading navigation can jump between brief sections. | src/lumen.jsx |
| 60 | UX | Internal tools UX + language mismatch (UX scan #3/#21/#22/#26/#27, all CONFIRMED). (a) HIGH: the Sales language dropdown offered Portuguese and Dutch, which the chat cannot render (UI_LANGS has 6) — the client was promised their language, then got an all-English chat. Dropdown now lists exactly the six supported languages, with an in-file note to extend the chat's I18N tables BEFORE adding options here. (b) Dashboard detail modal gets what the client modal got in v40: role=dialog + aria-modal, Escape-to-close, focus moves to Close on open, and focus returns to the triggering row on close (shared closeModal for button/backdrop/Escape). (c) Sales Copy button: a denied/unavailable clipboard no longer looks identical to success — falls back to execCommand copy, and failing that shows "Press Ctrl+C" with the link pre-selected, so a consultant can't unknowingly paste stale clipboard contents into a client email. (d) Removed the Sales footer note pointing at a "set the chat origin below" control that does not exist (CHAT_BASE is a deploy-time code setting). (e) Sortable dashboard columns now LOOK sortable at rest: a faint ⇅ on every sortable header (replaced by the real ▲/▼ when active); hover-only affordances are invisible until stumbled on and absent on touch. Verified via headless render: 7/7 sortable headers show the hint, non-sortable none, dialog semantics + Escape + Close-focus all pass, all prior dashboard assertions intact, sales inline JS syntax-checked, 6-language dropdown confirmed. | public/sales.html, public/dashboard.html |
| 61 | Feat | Support .docx upload in the QUERIES widget (Erin's onboarding-call feedback #3; Fortinet had only .docx/.pdf). A .docx is a ZIP whose body text lives in word/document.xml; we extract it with ZERO new dependencies: parse the ZIP central directory (authoritative compressed size, so a data-descriptor docx still works), find word/document.xml among all entries, inflate it with the browser-native DecompressionStream('deflate-raw'), and strip the XML to text (</w:p> -> newline, <w:br/> -> newline, tags dropped, XML entities decoded with & last). Security: a hard 12 MB cap on the DECOMPRESSED size (checked against the CD hint AND enforced while streaming) blocks a zip bomb; the existing 2 MB file-size and 200-line/15k-char caps still apply. Feature-detected: if DecompressionStream is missing (older browser), a localized message tells the client to paste the text or save as .txt instead. accept attribute + the import-file label + the unsupported/docxUnavailable messages updated in all 6 languages. Legacy binary .doc is NOT supported (not a zip); .pdf deferred (needs a heavy parser and report PDFs extract poorly). Verified end-to-end against the ACTUAL source functions with hand-built .docx zips: deflate, stored, data-descriptor, multi-entry (document.xml as the 4th of 5 like a real Word file), XML entities, <w:br/>, zip-bomb blocked, and non-zip garbage rejected cleanly (8/8). | src/lumen.jsx |
| 62 | Fix | Two adversarial-verification findings on v61 (.docx), both CONFIRMED medium. (a) PERF/mobile: the 12MB decompression cap was ~800x the 15k-char output the widget keeps, and the full inflated XML was regex-scanned (10 passes) before truncation — a highly-compressible or just-large .docx could clear the 2MB file guard, inflate to 12MB, and freeze/OOM a low-end phone. Now DOCX_MAX_XML is 1MB and inflateRawBounded takes a bounded PREFIX at the cap (stops inflating, cancels the stream) instead of throwing, so a large-but-real doc still imports its first chunk and capQueryText truncates + flags it; the regex pipeline never runs on a multi-MB string. Stored path also sliced to the cap; the pre-inflate uncompSize throw removed (the streaming cap supersedes it). (b) i18n/RTL: the Arabic import-file label was missing the U+200E LRM marks the same commit added to its sibling messages, so the extension list (incl. .docx) mis-rendered in RTL — added LRM before each extension to match. Re-verified: docx extraction 7/7 (zip bomb now bounded, not throwing), multi-entry intact, Arabic label carries 4 LRM marks. | src/lumen.jsx |
| 63 | Tool | Make the A/B harness produce a human-readable quality gate. It now writes ab-transcripts.txt (the full conversations, per config + run, with the recall tag) every run, so conversation quality can be judged by READING the actual transcripts instead of relying on the flaky auto-judge — the judge was parse-failing often and can't be trusted for the quality call. Added AB_ONLY (comma-separated config keys, e.g. AB_ONLY=baseline,full-history) to run a cheaper focused comparison. gitignored the output. This exists so the finding-21 / cost-lever quality decision rests on real transcripts a human (or Claude) reads, not a noisy score. Dry-run verified: AB_ONLY filters to the named configs and ab-transcripts.txt is written in readable form. | tools/ab-harness.mjs |
| 64 | UX | Design-backlog Batch 1: foundations + first impression + conversation feel (backlog A1,A2,B1,B2,B3,B5,B6,C1,C6,C7,F2,H1,H3,I1,I3,I4,K4,O1,P1). (a) A1 font flash: Inter now preloads via <link> in chat.html head (preconnect + display=swap) and the @import inside the React <style> was removed — clients no longer see Arial-then-reflow. (b) F2/B5: real purple-orb SVG favicon + a pre-JS branded splash inside #root (orb + "Loading your session…") that React replaces, killing the blank-white beat on slow loads. (c) H1/H3: a single design-token const T (radius/shadow/motion/text) added at top with an intent comment, so future visual changes are one edit; adopted in the new styles. (d) A2/P1/I1/I4/K4: the global <style> now defines motion tokens (--dur-* / --ease-out), universal button hover (translateY -1px + brightness) and active (settle + scale .985) states, ::selection purple, a .lm-theme colour transition on the root (dark/light eases in), and an Arabic-capable font stack for [dir=rtl] (system Geeza Pro/Noto, no extra web font). All still killed by the existing prefers-reduced-motion block. (e) B1/B2/B3: welcome hero staggers in (orb->title->pill->sub->langs->steps->disclaimer->CTA), the orb gets a radial halo + slow breathe (reduced-motion safe), language chips are promoted to the primary CTA (uppercase "Choose your language" label, 40px targets, selected chip glows, unselected text darkened for legibility), and seeded sessions show a "Prepared for {company}" pill. (f) B6: seeded tab title document.title = "Lumen Onboarding — {company}". (g) C1: the thinking indicator rotates contextual lines (Reading your answer -> Updating your setup brief -> Preparing the next step) every 2.6s with a crossfade, in all 6 languages, dots kept. (h) C6: send button always purple with the CTA glow when ready, dims to 40% when empty, desktop Enter-to-send hint. (i) C7: current stepper node gets a purple focus ring, completed check pops in. (j) O1: safe-area-inset-bottom padding on the composer for notched phones. New I18N keys chooseLang/preparedFor/think1-3 in EN/FR/DE/ES/IT/AR. Verified: production build clean (288KB initial, xlsx lazy 429KB), headless screenshots of welcome (light/dark/mobile), demo conversation, typed send-glow, and the export modal all render correctly with zero page errors. | public/chat.html, src/lumen.jsx |
| 65 | UX | Design-backlog Batch 2: client-facing conversation + trust (backlog C3,C5,C8,C10,C12,C14,F3,L2,L3). (a) C8 side panel: the "Captured so far" wall of identical "Not captured yet" rows is gone — only captured fields show (600 weight, each flashing purple once as it lands, the "the bot heard me" beat), everything still to come collapses to one calm line ("N more details will fill in as you chat") that reframes blanks as anticipation. (b) C14/L3 finish card: once the brief is actually sent, a drawn check-circle (stroke-dashoffset animation), a purple glow on the card, and a 3-step "what happens next" timeline (We review within 2 business days -> Your review call -> Go live) pay off the moment and set expectations; pre-send stays a plain call-to-action so a skimmer can't mistake it for done. (c) C5 resilience+polish: sendToAPI now does ONE silent auto-retry (600ms, spinner stays up, queued user turn preserved) before surfacing anything — clients forgive a hiccup they never see; and when the banner does show it is a proper amber warning card (wifi-off glyph, amber bg/border, dark-mode variant) with a primary purple Try again, not bare grey text. (d) C10 objectives: the #1 priority is now crowned (subtle purple row fill + bold), and the up/down/remove controls go 32->38px for easier tapping. (e) C3 bubbles: asymmetric corners (assistant 4/14/14/14 pointing to its avatar, user 14/4/14/14 pointing to the sender; symmetric kept in Arabic/RTL to avoid a wrong-side tail). (f) F3: auto-scroll no longer yanks a client who scrolled up to re-read — it only follows new content when they're already near the bottom, or when their own send kicks off the spinner. (g) C12: hover tooltips on the sound and theme toggles. (h) L2: a localized "Your answers are shared only with your Lumen onboarding team" reassurance line on the welcome screen (all 6 languages), the enterprise trust cue for clients pasting competitor lists. Deferred with reason: L4 (show consultant name) needs preparedBy exposed to the client, deliberately kept out of CLIENT_SAFE — a privacy call for the user. Verified: production build clean (291KB initial), headless screenshots of welcome+privacy line, demo conversation (asymmetric bubbles, dimmed send), and the captured-side-panel all render with zero page errors; sent-state finish card verified by build (reaching it needs the live serverless send). | src/lumen.jsx |
| 66 | UX | Design-backlog Batch 3: internal tools, Sales + Proserv dashboard (backlog D1,D2,D3,D4,D6,E1,E3,E4,E5 + A2). Grounding note: E2 (bold company, right-aligned tabular numerics, row hover) and N1 (the KPI stat-tile row) were ALREADY present, so this focused on what was actually unfinished. Dashboard: (E1) the raw browser window.prompt() for the access token is replaced by a styled in-page gate card (teal lockup, password field, Continue, inline error) — the backlog's single biggest "looks unfinished" fix, and the first thing a teammate sees; the 401-reauth path routes through the same card. (E3) the bare "No sessions yet" sentence becomes a real empty state (inbox glyph + title + "Sessions appear here the moment a client sends their brief"). (E4) the detail modal gets a sticky header (title + Close stay put through a long brief) and the migrated-queries block is capped at 220px with its own scroll (it can be 15k chars). (E5) Refresh now shows a "Refreshing…" disabled state and stamps "Updated HH:MM" next to the count instead of reloading silently. Sales: (D1) the flat form is grouped into "Client" and "Internal (never shown to the client)" sections. (D2) the generated-link panel slides down (max-height/opacity transition) instead of snapping, auto-selects the link, and Copy turns green "Copied ✓". (D3) alert()-based validation is gone: Generate is disabled until company + contact are filled, empty required fields show an inline red border + helper, and server errors render in an inline form-error line, never a browser alert. (D4) "Fill example" is demoted from a peer button to a quiet text link. (D6) a mobile media query stacks the paired rows and tightens padding. Both pages also get the chat's A2 hover/active button feedback, and the Sales write-token window.prompt() is replaced by the same styled card for consistency (no window.prompt anywhere in the tools now). Verified headless: both pages load with zero JS errors and zero alert dialogs; the dashboard gate renders and its submit flow works; Sales Generate is correctly disabled-until-valid, Fill-example enables it, and a failed generate shows the inline error (not an alert). | public/dashboard.html, public/sales.html |
| 67 | Fix+UX | Small-gaps pass (backlog J7, C7, D5, E4, B4 + FinishCard i18n; P4 verified already-correct). (a) J7 (trust): the review-modal error boundary rendered the raw exception text ("TypeError: ...") to the client in monospace — a technical string in front of an enterprise buyer erodes trust. It is now DEV-only; clients see just the reassuring "Your answers are safe…" line. (b) FinishCard localization: the finish card was hardcoded English, so a French/German/Arabic client hit an English wall at the single highest-trust moment (brief sent). New FN18N table + FN() localizes the titles, the "what happens next" timeline, the descriptions, and the Review/Send/Open-Sheet buttons in all 6 languages; the card now takes lang={uiLang}. (The dev-only "See what Proserv receives" button is not localized: it is null in the live LiveChat.) (c) C7: on mobile the six-dot stepper collapses to a single "Step N of 6 · Label" line with a thin fill bar, instead of a cramped dot row. (d) B4: the three welcome steps get purple-tinted icons (clock / chat bubble / paper-plane) in 32px rounded squares instead of plain number bullets. (e) D5 (sales): a helper line under Consultant notes ("Used to prepare the client's session: tier, priorities, key competitors…") and the notes section is now grouped under an "Internal (never shown to the client)" header. (f) E4 (dashboard): the detail modal's sticky header gains jump-to-section tabs (Topics / Channels / Users) that smooth-scroll the brief, with scroll-margin so the sticky header doesn't cover the target. (g) P4: verified the user's own message already renders synchronously before the API round-trip (optimistic UI) — no change needed. Verified: production build clean (296KB); welcome icons + privacy line + mobile single-line stepper screenshot correctly; dashboard + sales load with zero JS errors. | src/lumen.jsx, public/sales.html, public/dashboard.html |
| 68 | UX | Design-backlog Batch 4: foundations sweep (backlog A3-A8, H2, I2), scoped honestly. Grounding changed the plan: the "grey drift" and "radius chaos" the backlog assumed are largely stale or intentional in the current code. The bulk greys are already uniform (#e2e8f0 border 61x, #64748b muted 55x, #1e293b text 21x — one value each, not drift), and the remaining "strays" are deliberate: #616061/#ddd/#e8e8e8 live in the DEV-only demo cards that MIMIC Slack/Google UI, and the low-contrast greys on the Send button + rewind button are intentional disabled/DEV states. What was DONE: (A7) the one genuine client-facing stray — the review-modal Section disclosure arrow at #94a3b8 — darkened to #64748b (matches the ramp + AA). (H2) the chat's tokens are now mirrored as CSS custom properties (--radius-sm/md/lg, --shadow-raise/float/modal) in BOTH sales and dashboard :root, and applied to the cards/modals/gates, so the three surfaces provably share one system and a change propagates by name. (I2) modals animate in AND out instead of snapping: the dashboard detail modal fades + scales via a visibility toggle (0.16s, reduced-motion-safe), and the chat review modal gets a fade-in overlay + scale-in sheet on open (also now using T.radius.lg + T.shadow.modal). (A4/A5) radius + shadow TOKENS applied to the primary reachable surfaces (review modal, finish card, send button in earlier batches). DELIBERATELY DEFERRED, with reason (risk > value on a shipping build; the token layer from v64 is the real fix for future components): the full mechanical remap of all 124 borderRadius literals (mostly imperceptible 7-vs-8 differences; a blind global remap risks per-element regressions I can't fully verify pre-ship), the A3 type-scale remap of every fontSize (same risk, and the sizes are already within a sane range), A6 spacing, and A8 dark theming of the interactive INPUT widgets (ChipSelector/RankedSelector/UserForm etc. are light-only; the client-facing CHROME — header, bubbles, panels, stepper, finish/retry — all adapt to dark correctly, verified by screenshot). Verified: chat builds clean (296KB); welcome light/dark, dark conversation, and both internal pages render with zero JS/page errors; token vars resolve on sales + dashboard. | src/lumen.jsx, public/sales.html, public/dashboard.html |
| 69 | Fix | CRITICAL: topic suggestions leaked raw into the client's chat as TOPIC_SUGGESTION{...} JSON (reported from the live build; a Grammys client saw three raw JSON blobs). Root cause is a prompt/parser contract mismatch present in EVERY version: the system prompt tells the model to emit "TOPIC_SUGGESTION lines" but never specifies a delimiter, while every other structured field in the prompt is JSON (the %%TOPICS%% marker is {group,name,keywords,urls,hashtags,comments}); so the model naturally emits TOPIC_SUGGESTION{...json...}, but procTopics only matched the pipe form (TOPIC_SUGGESTION|name|keywords|rationale) and stripAll only stripped the pipe form — so the JSON was neither parsed into TopicCards NOR removed from the visible message. Fix (defense in depth): (a) procTopics now parses BOTH the JSON form (regex over flat TOPIC_SUGGESTION{...} objects, across newlines, mapping comments->rationale and carrying group/urls/hashtags) and the legacy pipe form, and removes both from the text; malformed JSON is still stripped so nothing can leak. (b) stripAll gained a matching JSON strip as a backstop, and now collapses the blank-line gaps left where markers were removed. (c) the system prompt now specifies the exact format (a single-line JSON object with the same fields as the TOPICS marker, not %%-wrapped, no pipes), described by reference so no risky quote-escaping entered the live prompt string. Verified: the REAL procTopics/stripAll (extracted from source) run against the EXACT screenshot reply parse all 3 suggestions, keep the boolean-query keywords (with escaped quotes), map rationale from comments, leave zero marker text in the visible message, keep the intro + closing prose, and still handle the legacy pipe form and malformed JSON (8/8). chat.js re-checked as valid JS; chat build clean. | src/lumen.jsx, netlify/functions/chat.js |
| 70 | Feat+Fix | Upload a supporting document at ANY point (Mckensey's feedback) + fix the failure she actually hit. Diagnosis: her "file too large" reading was wrong — the import SUCCEEDED (200 lines extracted); the SUBMIT failed. Root cause is that a supporting doc was only uploadable at the QUERIES step, and that path dumps the whole extracted blob (up to 200 lines / 15k chars) into a single chat turn, which makes the model's reply slow and times out the serverless round-trip (no extended function timeout is set), surfacing the generic "didn't go through". Fix, per the agreed design: (a) a persistent paperclip in the composer lets a client attach .txt/.csv/.xlsx/.docx at any time, not just at QUERIES. (b) the document is treated as CONTEXT, not a chat dump: the visible bubble is a clean chip (paperclip + filename, non-editable), while a BOUNDED excerpt (ATTACH_MAX_CHARS = 6000, ~1/2.5 of the old query cap) goes to the model wrapped in an instruction to PRE-FILL the current step and CONFIRM with the client, explicitly NOT to regurgitate — which keeps the reply short and the round-trip fast, removing the timeout. (c) sendToAPI now returns success and accepts an optional failMessage; an attach that still fails shows an honest one-off assistant message ("I couldn't read that document in time. It may be long. Try a shorter section, or paste the key part into the chat.") in the client's language, instead of the dead-end resend banner. (d) the extractor is refactored into a shared extractFileText() used by BOTH the QUERIES widget and the composer so they can't drift; QueriesWidget behaviour is unchanged. New AT18N table (label/trunc/failed) in all 6 languages; the model-facing framing stays English (the model still replies in the client's language). ALSO fixed a rendering bug this surfaced: the pre-send FinishCard emoji was written as JSX text 📨 (a v65 regression) which renders literally, not as an envelope — wrapped it in a string expression so React renders the glyph. Verified headless: paperclip present, attaching a .txt shows the clean chip (not raw text) and, with no API in the sandbox, the honest failure message (not the generic banner); zero page errors; FinishCard no longer shows the literal escape; production build clean (301KB). Model-side pre-fill behaviour to be confirmed in a live session. | src/lumen.jsx |
| 71 | Fix | Close the last gap on Mckensey's "file too large" failure. Context: her actual failure was a LARGE import submitted from the QUERIES widget timing out the round-trip and showing the dead "didn't go through" banner (not a real file-size rejection — the 2MB guard never fired; she misread the 200-line truncation note). v70 fixed her real use case (uploading a requirements DOCUMENT) via the bounded, framed composer attach. This closes the remaining gap in the QUERIES widget itself: a QUERIES submit now carries the same honest failure message as the attach path (AT "failed", localized) instead of the generic resend banner, so a genuinely large query export that times out tells the client to submit fewer at a time or hand over the whole doc via the paperclip, rather than dead-ending. The 15k/200-line queries cap is intentionally NOT lowered (queries can legitimately be long and truncating loses real data; the payload is already framed to keep the reply short). Note: a fully guaranteed fix for very large single submits is a longer serverless function timeout, which is a Netlify plan/infra setting, not a code change. Build clean. | src/lumen.jsx |
| 72 | Fix | Bug audit (3 parallel read-only passes: functions / client / internal+Apps Script), each finding verified against the code before fixing. Fixed the confirmed, unambiguous ones: (HIGH) dashboard.html stored XSS — the session row builder dropped client-POSTed percent/topicCount/channelCount/userCount into innerHTML unescaped (every other field was escaped/safeUrl'd); now coerced via num(), so a crafted session record can't run JS in the token-holding dashboard. (MED) Apps Script Slack injection — the threaded pipeline reply interpolated the client company name + account name raw, so a company named "<!channel>" fired a real broadcast; now slackEsc_'d like the base alert. (MED) Apps Script Sheets formula injection — client cell values (topics/users/channels/queries) written via setValue with no guard; added cellSafe_ (prefixes ' to a leading =+-@) in writeRows_ and fillQueries_, so "=IMPORTDATA(...)" can't execute in the client-shared Sheet or on CSV export. (MED) seed.js used crypto.randomUUID() with no node:crypto import (works on Node >=19, throws on 18) — added the import to match sheet.js. (MED) chat.js + sheet.js origin check only fired when Origin was present and called new URL(origin) unguarded (500 on Origin: null); now require Origin present AND matching with a try/catch, matching session.js/seed.js. (MED) widget Confirm/Skip had no in-flight guard (unlike sendMsg/handleSend), so a tap during a pending turn queued two consecutive user turns and 400'd (could wedge the session until they scrolled out of the 20-turn window) — added a synchronous busyRef guard in sendToAPI plus entry guards on onWSubmit/onWSkip/sendMsg/sendAttachment. (MED) TOPIC_SUGGESTION parser hardened again — the v69 flat-brace regex still leaked a payload containing a nested/quoted brace or a "TOPIC_SUGGESTION: {" colon prefix; replaced with string-aware brace counting (handles nested braces, colon prefix, and truncated/unterminated markers). (MED) the in-progress server autosave built company from cdata only, so the dashboard's stalled/in-progress view showed markets/objectives/teams/timezone blank though the client answered (they live in wState) — now merged in like the completed record. (LOW) loading aria-label used WL("thinking") which isn't in WI18N (returned "") -> L("thinking"); composer placeholder submitted-check used a bare widget key that never matched the "${index}-${type}" shape; seed.js SEED_TTL_DAYS NaN silently disabled expiry -> falls back to 90. Verified: build clean (302KB), all 5 functions + the .gs pass node --check, topic parser 10/10 (incl. new nested/colon/truncated/multi cases), docx extractor regression 8/8, headless smoke zero page errors. NOT fixed (need your decision / infra, reported separately): serverless function timeout on long completions (max_tokens 4000 vs Netlify ~10s — the systematic "didn't go through" root; infra timeout / lower ceiling / streaming), rate-limiting on the key-backed chat+sheet proxies, and setting SEED_WRITE_TOKEN/DASHBOARD_TOKEN in production. | public/dashboard.html, netlify/functions/{chat,seed,sheet}.js, apps-script/onboarding-sheet-webapp.gs, src/lumen.jsx |
| 73 | Fix | Timeout mitigation, the code half of the systematic "didn't go through" root cause (both audit passes flagged it; also behind Mckensey's large-import failure). The chat proxy is NON-STREAMING, so the model's entire reply must generate inside the serverless window (Netlify default ~10s, max 26s). The old MAX_TOKENS_CEILING of 4000 permitted ~45-65s of generation — a long reply was never truncated, the whole function was KILLED and the turn died. Changes: (a) ceiling 4000 -> 2000 in chat.js (client's maxTokens aligned; server clamps regardless). Sizing rationale in-code: the largest legitimate reply (full recap re-emitting every marker on a topic-heavy session, plus the <thought> block) measures well under ~1500 output tokens, so normal traffic is untouched; a runaway now ends as an API-level max_tokens truncation instead of a platform kill — and the system prompt already orders all %% markers BEFORE the visible prose precisely so a truncation costs trailing prose, not data; the client additionally has the dangling-marker silent retry + stripAll safety net. (b) An AbortController on the upstream Anthropic call (24s budget): a hung/slow upstream now returns a clean JSON 504 the client treats as a normal failure (silent retry, then the retry card) instead of an opaque platform kill; no-op under a 10s site timeout (platform wins the race), meaningful once the timeout is raised. (c) stop_reason==="max_tokens" now logged server-side so ceiling truncations are visible in function logs. (d) DEPLOY.md: new prominent step — raise the site's function timeout to 26s (Netlify UI), which closes the remaining default-10s gap for ordinary 700-1500-token replies; also replaced the stale v43-v47 "what changed" section with a pointer to versions/CHANGES.md plus deployment-relevant highlights (Apps Script redeploy now mandatory, function-timeout step, deploy dashboard.html together with the functions). Verified: chat.js passes node --check, client builds clean, no stray 4000 outside the explanatory comment, client/server ceilings aligned. NOT done here (still open, needs your call): streaming (the complete fix — removes the window entirely but is a larger change touching the %% marker protocol), and rate-limiting the key-backed proxy. | netlify/functions/chat.js, src/lumen.jsx, DEPLOY.md |
| 74 | Feat | Team-thread follow-ups 1-3 (Erin/Mckensey Slack discussion). (1) Early documents question (Mckensey's "initial prompt near the beginning"): new STEP 1.7 in the system prompt — right after the goal question, the assistant asks ONCE whether the client already has requirements/notes/lists written down and points at the composer paperclip (.txt/.csv/.xlsx/.docx); an attached doc then flows through the existing IMPORTED CONTENT rules (harvest every field, confirm in one message, skip covered questions). This closes the discoverability gap: the capability shipped in v70 but a guided-flow client was never told it exists. Also aligned the two stale STEP 3 file-type mentions ((.txt, .csv or .xlsx)) to include .docx, which v61 added everywhere else. (2) Attach excerpt cap 6k -> 12k chars: 6k was defensive sizing from before the v73 timeout work; what times a call out is OUTPUT length (now capped at 2000 tokens), not input — 12k chars of input is ~3-4k tokens, negligible latency, and covers a full multi-page requirements doc (the hackathon flagship case) instead of clipping it. Still bounded vs the 400k body cap across the 20-turn window. (3) Pre-work checklist on the Sales page (Erin's TAM/EM/IC toolkit ask): generating a link now also produces a personalized copy-paste prep snippet (Erin's 8 bullets generalized, company name woven in, closing line pointing at the paperclip + file types) with its own Copy button — generated WITH the link so it can never drift from what the assistant actually asks; English v1, translate as needed. Copy logic refactored to one shared bindCopy (link + checklist, same fallback chain + green Copied state); output panel reveal height raised to fit. Verified: chat.js node --check + STEP 1.7/docx greps; client build clean; headless sales run with a stubbed seed endpoint — link generated, checklist personalized (Acme in bullets 2/3/6), 8 numbered bullets, paperclip+docx mention, Copy shows "Copied", zero page errors, screenshot confirms layout. | netlify/functions/chat.js, src/lumen.jsx, public/sales.html |
